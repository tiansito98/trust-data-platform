# =============================================================================
# Trust Data Platform - dependencias del DASHBOARD (Streamlit Cloud)
# =============================================================================
# Esto es lo que Streamlit Community Cloud instala. Mantenerlo MINIMO: la app
# solo lee de Postgres, no toca Redshift ni hace SSH tunnels.
#
# Pipeline tiene su propio requirements-pipeline.txt (lo instala GitHub
# Actions, no Streamlit Cloud).
# =============================================================================

# Techo en 1.62: esa version reescribio st.date_input ("segmented fields and an
# accessible calendar", release del 2026-08-19) y el calendario perdio el
# selector de anio/mes y gano un desplegable "Date range". El dashboard depende
# de poder saltar de anio/mes en un solo gesto, asi que nos quedamos en la UI
# anterior hasta decidir si migramos.
streamlit>=1.57,<1.62
psycopg2-binary>=2.9.9
sqlalchemy>=2.0.0
pandas>=2.0.0
plotly>=5.18.0
bcrypt>=4.0.0
pyyaml>=6.0
openpyxl>=3.1.0           # Engine de pandas para xlsx_download_button
